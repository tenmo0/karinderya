// ========================================
// BACKEND SERVER CONFIGURATION
// ========================================
// Update this when you change networks
// This is just for display in console logs

// const SERVER_CONFIG = {
//   PORT: 5000,
//   HOST: "0.0.0.0", // Don't change this - it means "all network interfaces"
//   DISPLAY_IP: "192.168.18.3" // ← Change this to your current IP
// };

// module.exports = SERVER_CONFIG;


const SERVER_CONFIG = {
  PORT: 5000,
  HOST: "0.0.0.0",
  DISPLAY_IP: "172.20.10.4"  // ✓ Already correct
};

module.exports = SERVER_CONFIG;